name: root_config
description: A new Flutter package project.
version: 0.0.1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  injectable_generator: ^0.4.0
  build_runner: ^1.10.0
  json_serializable: ^3.3.0

flutter:
